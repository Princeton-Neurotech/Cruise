import { FunctionConfig } from '../../../config.js';
import { FeatureFlags } from '../../../feature_flags.js';
import { NodeBundler, NodeBundlerType } from './types.js';
export declare const getBundler: (name: NodeBundlerType) => NodeBundler;
export declare const getBundlerName: ({ config: { nodeBundler }, extension, featureFlags, mainFile, }: {
    config: FunctionConfig;
    extension: string;
    featureFlags: FeatureFlags;
    mainFile: string;
}) => Promise<NodeBundlerType>;
